# Server
NODE_ENV=development
PORT=4000

# SQLite (cale către fișierul bazei de date)
# Implicit: backend/data/db.sqlite
SQLITE_PATH=./data/db.sqlite

# JWT
JWT_SECRET=schimba-acest-secret-in-producție
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_EXPIRES_IN=30d
